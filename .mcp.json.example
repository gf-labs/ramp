{
  "_note": "Reference only — the MCP server is opt-in (not auto-registered). Enable it by running scripts/setup-mcp.py, or register manually: copy the mcpServers block and run `claude mcp add-json -s user knowledge-graph '<json>'`.",
  "mcpServers": {
    "knowledge-graph": {
      "type": "stdio",
      "command": "/path/to/ramp/mcp/start.sh",
      "args": [],
      "env": {}
    }
  }
}
